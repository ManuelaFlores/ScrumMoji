<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manuflowers.scrummoji">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".application.ScrumMojiApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.storyPointsResultsDev.StoryPointsResultsDevActivity"></activity>
        <activity android:name=".ui.pointsEstimator.PointsEstimatorActivity" />
        <activity android:name=".ui.storyPointsResults.StoryPointsResultsActivity" />
        <activity android:name=".ui.userStoriesFeed.UserStoriesFeedActivity" />
        <activity android:name=".ui.sprintsFeed.SprintsFeedActivity" />
        <activity android:name=".ui.roomLogin.RoomLoginActivity" />
        <activity android:name=".ui.jiraLogin.JiraLoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>